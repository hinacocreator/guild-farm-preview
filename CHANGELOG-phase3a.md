# Phase 3-A 変更履歴（サイト骨組み＋HOME本実装）

ベースライン: commit `dc1b58d`「v2.1 LP版」
対象: 1ページのLP → 6ページ構成のサイト＋HOMEの作り直し
この時点ではまだコミットしていません（レビュー後にコミット予定）。

**この文書の読み方**
「削除した」は本当に無くしたもの、「移動した」は別のページへ移したもの（＝サイトから消えていない）、
「データとして残した」は画面には出ないがファイルには残っているものです。
**コピー・価格・設備・FAQ などの原稿は、ひとつも削除していません。**

---

## 1. 残したもの（そのまま）

### デザイン資産
- 色（生成り paper / cream / sand、深緑 forest・moss、テラコッタ clay、土 soil、金 sun）
- 明朝の見出し（`.heading-jp`）、英字ラベル（`.label-en`）、通し番号（`.numeral`）、引用（`.pull-quote`）
- 紙の粒子（`body::after` のノイズ）、スクロールでのフェードイン（`.reveal`）
- 共通部品 `ui/Container` `ui/CtaButton` `ui/Photo` `ui/SectionHeading` `ui/LeafIcon`、`lib/jp.tsx` の `phrase()`
- 写真29枚（`public/images/`、`src/config/images.ts`）。**新しい写真は追加していません。**

### 原稿・設定
- `src/content/copy.ts` … Phase 2 のコピーを全文そのまま保持（下層ページが参照中）
- `src/config/site.ts` の `price`（85,300 / 58,300 / +25,000 / +35,000）、住所、最寄り駅、Instagram、`contactHref`
- 既存セクション部品（`sections/*.tsx`）はすべて残しています。HOMEから外したものも、下層ページで使うか、いつでも戻せる状態です。

---

## 2. 移動したもの（HOMEから外して、別のページへ）

| 内容 | 元（HOME） | 今の場所 | 備考 |
|---|---|---|---|
| 料金・含まれるもの | Value セクション | `/plans/` | 数字は `site.ts` の `price` が正 |
| 2つの滞在プランの詳細 | Plans セクション | `/plans/` | HOMEは「1週間 / 1ヶ月がある」ことだけ |
| 部屋・設備 | House セクション | `/plans/` | |
| 1日のスケジュール・体験の場所 | ADay / Schedule / Places | `/life/` | HOMEは朝昼夕夜の4場面だけ |
| 体験の詳細 | Experience セクション | `/life/` | |
| 考え方・目指す未来の全文 | WhyGuildFarm セクション | `/about/` | HOMEは大きな2行と引用だけ |
| 松井さんの話の全文 | Story セクション | `/owner/` | HOMEは2段落＋引用1つ |
| 申し込みの流れ・持ちもの | Flow セクション | `/flow/` | |
| FAQ | Faq セクション | `/plans/` | 金額を含む設問があるため、`/flow/` ではなく `/plans/` に置いています |

> いずれも「HOMEから外した」だけで、**サイトからは消えていません**。

---

## 3. 削除したもの（本当に画面から無くしたもの）

| 内容 | 理由 | 原稿の行方 |
|---|---|---|
| HERO下の「85,300円に含まれるもの →」リンク | HEROに金額を出さない方針 | `copy.hero.secondary` にデータとして残存（未使用） |
| スマホ固定CTAバーの価格表示「1ヶ月 85,300円」 | 料金は `/plans/` だけに出す方針 | `copy.stickyBar.price` に残存（未使用） |
| 肩書「まつやま瀬戸内農山漁村振興協議会 代表」 | **事実として誤り**のため、サイトから完全に削除 | 復活させないでください。肩書は「GUILD Farm オーナー」のみ |
| 「入居について相談する」という言い回し | 全て「滞在について相談する」に統一 | ページ名「入居までの流れ」だけは残しています |
| HOMEの `Journal`（H-08b ここでの毎日） | 設計書で任意のブロック。スマホでの総高さを優先（約800px 短縮） | 部品 `sections/Journal.tsx` はそのまま。`page.tsx` に1行足せば戻ります |
| HOMEの具体的な時刻（06:30 など） | 出典資料にない情報 | `copy.aDay` に残存（`/life/` で扱う） |

---

## 4. 新しく追加したもの

### ルート（下層ページ）
- `src/app/about/page.tsx` … GUILD Farmとは
- `src/app/owner/page.tsx` … 松井について
- `src/app/life/page.tsx` … 暮らし・過ごし方
- `src/app/plans/page.tsx` … プラン・料金
- `src/app/flow/page.tsx` … 入居までの流れ
（問い合わせページは作っていません。CTAは `contactHref` のメールへ）

### 共通の仕組み
- `src/app/layout.tsx` … ヘッダー / フッター / スマホ固定CTAバーを全ページ共通に。日本語フォントを Google Fonts の CSSリンク方式へ変更（`next/font` では日本語グリフが配信されず、明朝が再現できなかったため）。`preconnect` 付き。
- `src/components/SiteHeader.tsx` … 作り直し。スマホはハンバーガー、開いたメニューは深緑の面に生成りの明朝。`aria-expanded` / Esc で閉じる / 背面スクロールの固定。現在ページを強調。
- `src/components/SiteFooter.tsx` … 下層ページへのリンクを追加。
- `src/components/StickyCtaBar.tsx` … 価格表示を削除。`#top` が無いページは先頭から表示、`#contact` が無いページは出しっぱなし。
- `src/components/PageHeader.tsx` / `PageOutline.tsx` … 下層ページ共通のページ見出しと目次。
- `src/content/pages.ts` … 下層ページのタイトル・説明文・リード・見出し一覧。
- `src/config/site.ts` … `navItems`（下層URL）と `ctaLabel: "滞在について相談する"` を追加。`price` は保持。
- `src/app/globals.css` … 日本語フォントの変数定義を `:root` に移設（変数名 `--font-zen-kaku` / `--font-shippori` は据え置き）。

### HOME（②HOME本実装）
- `src/content/home.ts` … **HOMEの文字はすべてここ**。`copy.ts` とは別ファイルです（`copy.ts` は下層が参照中のため触っていません）。
- `src/components/ui/TextLink.tsx` … 「次のページへ送る」テキストリンク（下線＋矢印）。明・暗の2トーン。
- `src/components/sections/home/` … HOME専用の短縮版セクション。既存の About / ADay / Story などは下層で使うため**変更していません**。
  - `HomeIntro.tsx`（H-02）／`HomeValues.tsx`（H-03）／`HomeDay.tsx`（H-04）
  - `HomeOwner.tsx`（H-05）／`HomeFuture.tsx`（H-06）／`HomeStays.tsx`（H-07）／`HomeVoices.tsx`（H-08）

### 変更した既存ファイル（HOMEのため）
- `src/components/sections/Hero.tsx` … HOME専用のため作り替え。`home.ts` を参照。金額リンクを削除し、主CTAを `/life/`、副リンクを `/about/` に。`hero` / `heroMobile` の `<picture>` 出し分けと `id="top"` は維持。
- `src/components/sections/ClosingCta.tsx` … 見出し・本文・ボタン・注記を props で差し替えられるように（既定値は `copy.closing` のままなので、下層5ページの表示は変わりません）。「次に読む」副リンク用の `next` props を追加。`id="contact"` は維持。
- `src/app/page.tsx` … HOMEの並びを H-01〜H-09 に差し替え。

### どのページからも呼ばれていない部品（消していません）
Phase 2 のコピーを保持するため、ファイルは残してあります。使いたくなったら `import` して置くだけです。

| 部品 | 中身 | 今後 |
|---|---|---|
| `sections/About.tsx` | INTRO「住まい／畑／仕事」の3本柱 | `/about/` 本実装の材料 |
| `sections/Journal.tsx` | Instagram写真6枚（H-08b） | HOMEに戻すか、将来の `/journal/` で |
| `sections/CtaBand.tsx` | ページ途中のCTA | Phase 2 時点から未使用 |

---

## 5. HOMEの最終構成

| # | ブロック | 見出し | 写真 | 出口 |
|---|---|---|---|---|
| H-01 | Hero | 暮らしの中に、農がある。 | hero / heroMobile | `/life/`（主）・`/about/` |
| H-02 | HomeIntro | 旅行ではなく、暮らしてみる。 | aboutField | `/about/` |
| H-03 | HomeValues | この1週間、この1ヶ月に、起きること。 | expFarmwork / expFood / dayWork / expPeople / aboutSoil | `/life/` |
| H-04 | HomeDay | ここでの、1日。 | dayMorning / dayWork / dayEvening / dayDinner | `/life/` |
| H-05 | HomeOwner | この場所を、はじめた人。 | journal[5]（※本人写真ではありません） | `/owner/` |
| H-06 | HomeFuture | 畳1畳の畑から。／半径2kmの、農的社会コミュニティ。 | whyVisual | `/about/#future` |
| H-07 | HomeStays | 1週間か、1ヶ月か。 | expHarvest / house[0] | `/plans/` |
| H-08 | HomeVoices | （画面に出す見出しなし。読み上げ用に「この場所で生まれた言葉」） | （写真なし） | `/owner/` |
| H-09 | ClosingCta | 農のある暮らしを、少し体験してみる。 | cta | 問い合わせ（＋`/plans/`） |

- スマホ（375px幅）での総高さ **11,007px**（Phase 2 のLPは約20,000px）。※ QA後の実測値
- H-04 はスマホで横スクロール＋スナップ、PCで4列。
- H-03 は写真の大小を混ぜた編集的な並び（PCは12列で 7+5 / 5+4+3）。

---

## 6. 未確認・仮置きの箇所（推測で埋めていません）

| 箇所 | 内容 |
|---|---|
| `home.ts` の `owner.name`「松井 真弥」 | **氏名の正式表記とふりがなが未確認**。現サイトからの引き継ぎです |
| H-05 の写真 | **松井さん本人の写真がありません**。田んぼの風景写真で代用し、本人でないことをキャプションで明記。コード内に「★ 松井本人写真差し替え推奨」と記載 |
| `site.ts` の `contact.formUrl` | 空。Googleフォームの有無・問い合わせ運用が未確定のため、CTAはすべてメール（mailto）です |
| 見学の有無・固定された滞在日程 | 資料にないため書いていません（「最初の週」「最後の2日」等の表現も未使用） |
| H-08 の3つの言葉 | 原文のまま。属性は付けていません。**掲載許諾の状況は未確認**のため、人物写真は添えていません |
| `/about/` `/owner/` `/life/` `/plans/` `/flow/` の本文 | 骨組み（見出し＋1行要約）まで。本実装は Phase 3-B 以降 |
| `copy.story.paragraphs` | Phase 2 からの仮テキスト（本人の言葉ではありません）。`/owner/` の本実装時に差し替えが必要です |
| Faq の配置 | 金額を含む設問があるため `/plans/` 側に置いています。`/flow/` との振り分けは本実装時に再検討 |

---

## 7. 確認済み（静的チェック）

- `npm run lint` … エラーなし
- `npm run build` … 成功（9ページを静的書き出し）
- `out/index.html` に以下が **0件**: `85,300` / `58,300` / `25,000` / `35,000` / 「入居について」/ 「振興協議会」/ `06:30` 等の時刻表記／「◯◯円」の表記
- `out/index.html` に `/life/` `/about/` `/owner/` `/plans/` `/about/#future` へのリンクあり
- 横スクロール（意図しないはみ出し）なし：375px / 1440px の両方で確認
- 下層5ページの末尾CTAは、既定の文言（`copy.closing`）のまま変わっていないことを確認

---

## 8. ブラウザ検証（QA）で見つけた不具合と、その修正

実機に近い形（Chrome / スマホ375px・PC1280px）で全ページを開いて確認し、
見つかった不具合をその場で直しました。**文章の追加・事実の追加はしていません。**

| # | 症状 | 原因 | 直したファイル |
|---|---|---|---|
| 1 | HOMEの「GUILD Farmとは →」から `/about/#future` へ飛ぶと、**ページの先頭に着地して目的の見出しまで動かない** | `globals.css` の `html { scroll-behavior: smooth }`。これが付いていると Next.js のアンカー移動が効かなくなります。ページ内リンクは無いので、なめらかスクロール自体が不要でした | `src/app/globals.css` |
| 2 | アンカーで着地したとき、見出しが**画面上部の固定ヘッダーの裏に隠れる** | 一覧の項目に `scroll-mt`（着地位置の余白）が無かった | `src/components/PageOutline.tsx` |
| 3 | PCのナビで、**今いるページの色が変わらない**（下線だけ） | `text-ink-soft` と `text-clay` を同時に渡していて、あとに出てくる `text-ink-soft` が勝っていた。どちらか一方だけ渡すよう分岐を直した | `src/components/SiteHeader.tsx` |
| 4 | スマホで見出しが**1文字だけ次の行に落ちる**（「自分の暮らしを考え／る」「農的社会コミ／ュニティ。」） | 日本語の禁則処理と文節での折り返しを指定していなかった | `src/app/globals.css`（`body` に `line-break: strict`、`.heading-jp` に `line-break: strict` と `word-break: auto-phrase`） |
| 5 | 短い文が語の途中で割れる（「必要もありませ／ん。」「暮らしてみてく／ださい。」「滞在先は道後の家で／す。」） | 同上。短くて目立つ文のための `.wrap-phrase` クラスを追加し、CTAの本文と注記／HEROの説明文／各ブロックのリード一文／H-07のカード本文に適用しました。長い段落には付けていません（行末の余りが大きくなるため） | `src/app/globals.css` / `ClosingCta.tsx` / `Hero.tsx` / `PageHeader.tsx` / `home/HomeIntro.tsx` `HomeValues.tsx` `HomeDay.tsx` `HomeStays.tsx` |
| 6 | HEROで**同じことを二度言っていた**（「1週間から1ヶ月」のすぐ下に「1週間、または1ヶ月。」） | 重複していた1行を外しました（滞在の長さは上の一文で伝わります）。事実は減っていません | `src/content/home.ts` |
| 7 | H-08「この場所で生まれた言葉」に**見出しが無く**、読み上げソフトや目次で位置が分からない | 画面には出さない見出し（`sr-only`）を1つ置きました。文言は設計書のブロック名どおり | `src/components/sections/home/HomeVoices.tsx` |
| 8 | `CtaButton` のコメントに古い文言「入居について相談する」が残っていた | コメントのみ修正 | `src/components/ui/CtaButton.tsx` |

### 確認して「問題なし」だったもの
- 日本語フォント … 見出しは明朝（Shippori Mincho）、本文はゴシック（Zen Kaku Gothic New）で表示。Google Fonts の CSS も読み込めています
- ハンバーガー … 深緑の面・生成りの明朝、開閉、Escで閉じる、項目をタップすると遷移して閉じる、開いている間は背面がスクロールしない、現在ページが金色、`aria-expanded` あり
- スマホ固定CTAバー … HOMEはHEROを過ぎてから表示、最終CTAが見えると消える。下層ページは先頭から表示
- H-04 の横スクロール … スナップ付きで動作（4枚）
- 横スクロール（意図しないはみ出し）… 6ページすべて 375px / 1280px で 0
- 写真 … 全ページ読み込み成功・alt あり・切れや黒帯なし。スマホのHEROは `hero-mobile.jpg`
- 料金の数字 … `/plans/` 以外のページには 1つも出ていません
- 「入居について相談する」「振興協議会」等の誤肩書 … 全ページで 0件
- コンソールエラー … 6ページとも 0件
- 下層ページのヘッダー … 最初から生成り（HOMEだけ写真の上で透明）

### 直していない（判断が必要な点）
- `/life/` に **具体的な時刻（06:30 など）** が残っています。Phase 2 の `copy.aDay` / `copy.schedule` をそのまま置いているためです。「データを消さない」方針を優先して残しましたが、出典資料にない数字なので、Phase 3-B で**時刻を外すか、松井さんに確認して確定させる**必要があります
- `/flow/` に **「見学する」というステップ**が残っています。見学の有無は未確定です。同上の理由で残しています
- 下層ページで、**一覧（Contents）の見出しと、その下の既存セクションの見出しが重複**しています（例: `/about/` の「見るのでも、体験するのでもなく。」が2回）。骨組みとして既存セクションを残しているためで、本実装で一覧を実際のセクションに置き換えると解消します
- `/plans/`（16,047px）と `/life/`（13,961px）が **HOME（11,007px）より長い**状態です。既存セクションをそのまま積んでいるためで、本実装で整理が必要です
- `/about/` は写真2枚、`/flow/` は1枚と、**下層ページは写真が少なく文字中心**です。「写真主体」というサイトの性格に合わせるなら、本実装で既存29枚から配分し直すのが望ましいです

---

# Phase 3-B QA（全6ページ通し検証）

QA兼編集者として、6ページを 375px / 1280px で通しで確認し、見つかった問題をその場で直した記録です。
※ 検証環境の制約で、ブラウザのプレビュー枠が非表示状態だったためスクリーンショットが撮れませんでした。
　代わりに `getBoundingClientRect` と `getComputedStyle` で位置・大きさ・折り返しを数値で確認しています。

## A. テキストの修正

### A-1. 事実の誤り（出典に無いことを書いていた箇所）

| # | ページ | 修正前 | 修正後 | 理由 |
|---|---|---|---|---|
| 1 | ABOUT 01 | 引用「全員が農家になる必要はないと思っています。それでも、食べものが生まれる場所を知っている人が増えることには意味がある。」 | 引用「やりたい人全員みな農家、を描いているので笑」 | **松井さんが言っていない文を、引用として出していました**。しかも内容が原文（「やりたい人全員みな農家、を描いているので笑」）と逆向きです。原文の言葉に差し替えました |
| 2 | HOME H-06 | 「全員が農家になる必要はないと思っています。それでも、食べものが生まれる場所を知っている人が増えることには意味がある。」 | 「半径2kmは、歩いて行き来できる距離です。その範囲に、自分が食べるものを少しでもつくっている人が何人もいる。そんな状態を考えています。」 | 同上（HOMEにも同じ文が入っていました） |
| 3 | ABOUT 03 | 「**代表の松井**が気にしていることを」 | 「**オーナーの松井**が気にしていることを」 | 肩書は「GUILD Farm オーナー」だけ、という決まりに反していました |
| 4 | ABOUT 03 | 「**5年以上考えて出した答え**が、「農的暮らしが幸せ」だと感じる人を増やすことでした」 | 「**そこから考えて出てきた答え**が、〜」 | 資料の「5年以上」は〈生産以外の仕事を作れないか悩んだ期間〉のことで、この答えに掛かる年数ではありません |
| 5 | FLOW FAQ「どんな人が向いていますか」 | 「**設備の新しさや快適さを第一に選ぶ方には、向かないかもしれません。**」 | 「経験や年齢での線引きはしていません。気になることがあれば、相談のときに正直にお伝えします。」 | 資料に無い「向かない人」を作っていました。原文は「大前提どんな人にもです」です |
| 6 | LIFE 03・05 | 「**海の近くの**畑と果樹園」 | 「**梅津寺（ばいしんじ）の**畑と果樹園」 | PLANS・FLOWでは「梅津寺の畑と果樹園」と書いており、呼び方がページごとに違っていました。地名のほうが初めての人にも分かります |

### A-2. HOME と ABOUT で同じ引用が重複していた

HOMEの「松井について」と ABOUTの「豊かさと、心地よさ」に、**まったく同じ引用**（「今までは不安や恐怖にフォーカスして動いてきましたが、今は豊かさや心地よさにフォーカスして動いています。」）が入っていました。
HOME側を残し、**ABOUT側を matsui-story.txt の別の原文**に差し替えました。

> 1ヶ月という貴重なじかんで、自分の理想とする暮らしを実現するきっかけを掴む方が増え、その先に食の安全や心の豊かさが実現されたら最高やん！と思って営んでいます。

あわせて、この引用と内容がかぶっていた本文2段落も書き直しています。

### A-3. ルール2（AI的な“意味深コピー”）の出現数

「Aではなく、B。」「〜ではありません。」「答えではなく／問い」「少し長い話」の、**本文に出ている数**（コメント行は除く）です。

| ページ | 修正前 | 修正後 | 主に直した箇所 |
|---|---|---|---|
| **HOME** | **11** | **2** | 下表のとおり |
| ABOUT | 2 | 2 | （上限内。ページ見出しと「見るのでも、体験するのでもなく。」の2つだけ） |
| OWNER | 1 | 0 | 「そのとき生まれたのは、安心ではありませんでした。」→「そのとき浮かんだのは、心配のほうでした。」 |
| LIFE | 3 | 2 | 「用意されたプログラムを消化するのではなく、〜」→「決められた作業メニューはありません。住んでいる人として、一緒に手を動かします。」 |
| PLANS | 2 | 2 | （「家賃ではなく、暮らしそのものの会費」は指示書で認められた例外として残置） |
| FLOW | 1 | 1 | （利用者の質問文「人付き合いが得意ではありません。」のみ。対象外） |

HOMEで書き直した文（上から順）:

| 場所 | 修正前 | 修正後 |
|---|---|---|
| H-02 サブ | 見るのでも、体験するのでもなく。 | 1週間か1ヶ月、畑のある家に住んでみる。 |
| H-02 本文1 | **観光地を回るのではなく、**朝起きて畑へ行き、〜 | 朝起きて畑へ行き、昼から仕事をして、夕方また外へ出る。〜 |
| H-02 本文2 | **宿に泊まるのではありません。**個室のある家に住み、〜 | 泊まるのは、1人1部屋の個室がある家です。共用のキッチンで自炊をして、近所の人と顔を合わせます。 |
| H-03 サブ | 旅行は地域を見る。農業体験は農業を体験する。**ここでするのは、そのどちらでもありません。** | 畑に出て、その日採れたものを食べて、地域の人と顔を合わせる。滞在のあいだに起きるのは、だいたいこの5つです。 |
| H-03 土に触れる | 種をまく、草を取る、収穫する。**見学ではなく、**手を動かす側で。 | 種をまく、草を取る、収穫する。作業をする人として、畑に入ります。 |
| H-03 人とつながる | **紹介されて会うのではなく、**作業をしているうちに顔見知りになっていく。 | 畑で一緒に作業をしているうちに、地域の人と顔見知りになっていきます。 |
| H-03 自分の暮らしを考える | **答えを出す場所ではありません。問いを持ち帰る場所です。** | 移住するか、二拠点にするか、今のままでいくか。決める前に、暮らして確かめられます。 |
| H-05 本文1 | 〜できたとき、**生まれたのは安心ではなく、心配でした。** | 〜できたとき、浮かんだのは心配のほうでした。 |

※ H-02の「この場所は、不安からではなく、豊かさや心地よさからつくられています。その理由は、少し長い話になります。」は、Phase 3-A 指示書で「必ず入れる」とされた一文のため残しています（これがHOMEに残した2つのうちの1つです）。

### A-4. 直さなくてよいと判断したもの

- LIFE「木曜 … 午後は見学」… 出典（FACTS v2 1週間の流れ）どおりなので残置。FLOWの5ステップに「見学する」は入っていません（確認済み・脚注で ※要確認 のまま）
- 料金の数字（85,300 / 58,300 / +25,000 / +35,000）… `/plans/` 以外のページに 1つも出ていません（全文検索で確認）
- 個人名（山岡・チロポン・みどり・滞在者名）… 全ページ 0件
- 資料にない時刻（06:30 等）… 全ページ 0件
- ルール1（何をする場所か分かるか）… 6ページとも、情緒的な見出しの直後に「実際に何をするか」の説明が入っていることを確認しました

### A-5. 表記の統一

- ClosingCta の「次に読む」ラベル … LIFEだけ全角コロン（次に読む：）だったので、他と同じ半角コロン（次に読む: ）に揃えました

## B. 表示の修正

### B-1. スマホで1文字だけ次の行に落ちていた（OWNER）

引用「嫌な雑味なく香りが強い味が好き美味しい」が、375px で最後の「い」だけ次の行に落ちていました。
引用は原文どおりで文言を変えられないため、**文字サイズと左の余白**で直しています（`text-[1rem]`→`text-[0.95rem]`、`pl-6`→`pl-5`。PCは変更なし）。
→ 修正後、6ページすべてで「1文字落ち」は 0件です。

### B-2. /plans/ と /life/ が長すぎた（>16,000px）

情報は1つも削らず、**写真の見せ方と余白**だけで縮めました。

| ページ | 修正前 | 修正後 | 減った分 |
|---|---|---|---|
| /plans/ | 18,151px | **15,991px** | −2,160px |
| /life/ | 17,409px | **15,907px** | −1,502px |

やったこと:

1. **`Photo` に `ratioMobile` を追加**（`src/components/ui/Photo.tsx`）
   スマホのときだけ写真の縦横比を変えられるようにしました。PCの見た目は変わりません。
   縦長（4:5）のままだと1枚で 419px あり、それが何枚も続くとページが極端に長くなるためです。
   使った箇所: LIFE の「ある1日」2枚 /「1週間」1枚 /「畑のほかの時間」5枚、PLANS の「働きながら、住む」1枚・「含まれるもの」1枚 → いずれもスマホだけ 3:2（223px）に。
2. **PLANS「住むのは、こんな家です。」の写真6枚を、スマホでは2列に**（PCは従来どおりのずらし配置）
   6枚を縦に並べると写真だけで約3,400pxありました。2列にして、キャプションは番号を上に積む形に変更（狭い幅でも読めるように）。外観の1枚だけはスマホでも横いっぱいにしています。
3. **この2ページのセクション余白だけ、スマホで `py-20`（80px）→ `py-16`（64px）**。PC（`md:py-32`）は変更なし。
4. 同じくこの2ページの、項目リストの上下余白と段落間の間隔を1段階だけ詰めました。

※ 3・4 は /plans/ と /life/ にだけ入れています。他の4ページは `py-20` のままです（長さの問題が無いため）。

### B-3. 確認して「問題なし」だったもの

| 項目 | 結果 |
|---|---|
| 横スクロール | 6ページ × 375px / 1280px すべて 0（`scrollWidth` = ビューポート幅、はみ出し要素 0件）。HOMEのH-04は意図した横スクロール（スナップ付き）です |
| 総高さ（375px） | HOME 11,036 / ABOUT 13,999 / OWNER 14,491 / LIFE 15,907 / PLANS 15,991 / FLOW 9,713 |
| 写真 | 29枚すべて HTTP 200 で配信。alt あり。比率は PC で 4:5 / 3:2 / 1:1 とも指定どおり |
| ハンバーガー | 5ページへのリンク＋Instagram＋メール＋CTA。`aria-expanded` が切り替わり、開いている間は背面がスクロールせず（`overflow: hidden`）、Escで閉じる |
| スマホ固定CTAバー | HOMEはHEROを過ぎてから表示（HERO上は `aria-hidden="true"`）、下層ページは先頭から表示、末尾CTAが画面内のときは隠れる |
| 「次に読む」リンク | about→/life/ ／ owner→/plans/ ／ life→/plans/ ／ plans→/flow/ ／ flow→なし ／ HOME→/plans/。設計どおり |
| `/about/#future` | 目的の見出しまで移動し、固定ヘッダー（65px）の下に着地（`scroll-margin-top: 64px`） |
| 見出しの1文字落ち | 6ページとも 0件 |
| `npm run lint` / `npm run build` | どちらも成功（9ページを静的生成） |

## 直していない／判断が必要な点

1. **コンソールエラーの最終確認ができていません。** ブラウザの記録に「hydration mismatch」のエラーが5件残っていましたが、中身を見ると `WhyGuildFarm` `Story` `Experience` `House` `Value` という **Phase 3-A 時点にしか存在しないセクション名**が入っており、古い記録です。いまのサーバー出力を直接確認したところ、エラーの原因とされていた `loading` 属性は正しく `lazy` で出ています。新しく読み込み直した状態ではエラーは出ませんでしたが、プレビュー枠が非表示だったため、**実機のブラウザでもう一度コンソールを確認することをおすすめします**。
2. **スクリーンショットによる目視確認ができていません**（同じくプレビュー枠が非表示のため）。写真の切れ方・余白のリズム・2列にしたPLANSの家の写真の見え方は、数値上は問題ありませんが、**実機での目視を1度お願いします**。
3. **`src/components/PageOutline.tsx` が未使用のまま残っています**（本実装で各ページから外れたため）。データ（`src/content/pages.ts` の `outline`）を消さない方針なので、こちらも残置しました。消してよいかの判断が必要です。
4. **松井さんの氏名表記・ふりがな**（「松井 真弥」）は引き続き ※要確認 のままです。
5. **`/plans/` と `/life/` だけ、スマホのセクション余白が他ページより 16px 狭い**状態です（B-2の3）。6ページで完全に揃えるなら、他の4ページも `py-16` にするか、この2ページを `py-20` に戻して別の方法で縮める必要があります。

---

# Phase 3-C レビュー修正①

クライアントレビューの4点を反映しました（2026-09-14）。料金の数字は /plans/ のみ・肩書は「GUILD Farm オーナー」のみ・資料にない事実は追加しない、という共通ルールは維持しています。

## 1. FLOW 冒頭の説明文を自然な文章に（`src/content/flow.ts`）

- ページヘッダーのリード1段落目を差し替えました。
  「滞在を考え始めてから、GUILD Farmでの暮らしが始まるまで。ご相談から滞在開始までの流れをご紹介します。」
  （2段落目の「日程も期間も、決まっていないままで構いません。…」はそのまま残しています）
- 削除した「下の流れは目安です。時期や部屋の空き状況によって前後します。料金は「プラン・料金」のページにまとめています。」の**注意事項は捨てずに移動**しました。
  - 「流れは目安／時期や部屋の空き状況で前後する」→ `steps.lead`「連絡から、到着の日まで。下の流れは目安で、時期や部屋の空き状況によって前後します。」
  - 「料金は「プラン・料金」のページに」→ ステップ03「申し込む」の説明文の末尾（該当ステップの補足）。セクション下部の /plans/ へのリンクも従来どおりです。
- 不自然な「先に〜」「〜しておきます」調を解消しました。
  - セクション見出し「先に知っておいてほしいこと。」→ **「知っておいてほしいこと。」**
  - その導入「あとから「そうだったのか」にならないよう、書いておきます。」→「来てから分かると戸惑いやすい3つのことを、あらかじめお伝えします。」
  - 併せて `src/app/flow/page.tsx` と `src/components/sections/flow/FlowConcerns.tsx` のコメント内の旧見出し名も直しました（表示には出ません）。

## 2. ABOUT「実際にすることは、こうです。」を差し替え（`src/content/about.ts`）

- `difference.detailLead` / `detail` を `difference.practice` に作り替えました。
  - 見出し … **「自分らしい、農との関わり方を見つける。」**
  - 本文 … 「毎日畑に出る必要はありません。自分のペースで畑に通ったり、仕事を続けながら農に触れたり、ときには地域を巡ったり。ここでの時間の使い方は、それぞれです。暮らしの中に農がある時間を過ごしながら、自分に合った農との関わり方を探していけます。」
  - その下に「たとえば」として、**農・仕事・暮らし・学び・観光**の5項目を並べました。中身は従来の本文にあった事実（朝夕の畑作業／部屋の机での仕事／共用キッチンで採れたものを食べる／週1回の技術を教わる日／雨の日は畑の予定がなくなる）を組み替えただけで、**事実は追加していません**。
  - 「決まった時間割はありません。滞在のはじめに…一緒に組み立てます。」は注記として維持。
- 併せて、このセクションのサブコピー「旅行や農業体験と何が違うのか、先に書いておきます。」→「旅行や農業体験と、どこが違うのかを並べてみました。」

## 3. 「やりたい人全員みな農家」の引用を ABOUT から削除

- `about.difference.quote`（「やりたい人全員みな農家、を描いているので笑」）と、`AboutDifference.tsx` の引用ブロックを削除しました。
- 後ろに続いていた「増やしたいのは、農に関わる人です。」は、罫線＋リーフアイコンの締めの一文として残し、前後がつながるようにしています。
- **OWNER の「どんな人に来てほしいか」の引用はそのまま**です（背景説明があるため。`src/content/owner.ts` は未変更）。

## 4. 「旅行／一般的な農業体験／GUILD Farm」の比較表を追加

- データ … `src/content/about.ts` の `difference.compare`（列見出し3つ＋比較軸5つ。文言・◎○△ はクライアント指定のまま）。
- 表示 … **新規 `src/components/sections/about/AboutCompare.tsx`**。`AboutDifference`（深緑の面）の見出し直後に置き、その下に上記2の「自分らしい、農との関わり方を見つける。」が続く構成です。
- 実装のポイント
  - セマンティクスのため `<table>`（比較軸は `<th scope="row">`、画面に出さない `<caption>` あり）。`overflow-x:auto` のコンテナに入れているので、**横スクロールはページ全体には出ません**。
  - PC（md 以上）は4列の表。GUILD Farm 列だけ `bg-paper/8`（生成りの薄い面）＋ `border-sun/40〜50` の罫線で強調。深緑背景なので文字は paper 系です。
  - スマホ375pxは4列を押し込まず、**比較軸ごとのブロック**（軸名 → 旅行／一般的な農業体験／GUILD Farm の3行、GUILD Farm 行を強調）に変わります。Tailwind の display ユーティリティ（`block` ↔ `md:table-cell`）での切り替えです。

## 確認したこと

- `npm run lint` … エラーなし。`npm run build` … 成功（9ページ静的生成）。
- `out/about/index.html` … 比較軸5つ（滞在／農との関わり／過ごし方／地域との関係／自由度）がすべて出力。「やりたい人全員」は 0件、`out/owner/index.html` には残存（1件）。
- `out/flow/index.html` … 「先に書いておきます」「先に知っておいて」ともに 0件。
- 料金の数字（85,300 / 58,300 / 25,000 / 35,000 / 〜円）… /about/ /flow/ ともに 0件。
- ブラウザでの目視確認は行っていません（指揮者側で確認）。

---

# Phase 3-C レビュー修正②

「GUILD Farm は決められたプログラムを提供して案内し続ける場所ではない／畑でできることは松井から連絡するが、それ以外の時間は基本的に自由」という体験価値の核を、サイト全体のトーンとして反映しました（2026-09-14）。採用要件的・排他的な言い方（「主体性のある人を求めています」等）は使っていません。料金の数字は /plans/ のみ・資料にない事実は追加しない、という共通ルールは維持しています。

## 1. ABOUT に新セクション「こんな過ごし方を楽しめる方に。」を追加

- 文章 … `src/content/about.ts` の **`fit`**（id="fit"）。見出し・本文2段落・補足はクライアント確定の文案どおりで、事実は足していません。
- 表示 … **新規 `src/components/sections/about/AboutFit.tsx`**。写真は `expNature`（手入れをしている竹林／「畑以外の時間もある」ことが伝わる風景）。背景は `bg-cream`（前が深緑の `AboutDifference`、後ろが `bg-paper` の `AboutAgrarian` なので、面が続かないようにしています）。左に写真・右に文章の12カラムグリッドで、既存 ABOUT セクションのレイアウトを踏襲しています。
- 配置 … `src/app/about/page.tsx` で **`AboutDifference`（＝「自分らしい、農との関わり方を見つける。」を含むセクション）の直後**、`AboutAgrarian` の前。
- 補足（「反対に、毎日の予定や体験内容が…少し合わないかもしれません。」）は、本文より**小さく薄い文字**（`text-[0.85rem]` / `text-ink-faint`）で罫線の下に添えました。枠や警告色にはしていません。
- セクションの通し番号を振り直しました（`fit` = 02、以降 agrarian 03 / why 04 / fertilizer 05 / food 06 / people 07 / abundance 08 / future 09）。**`id="future"` は変えていません**（HOMEからの着地点のため）。

## 2. FLOW の FAQ「どんな人が向いていますか。」を短縮＋/about/#fit へ誘導

- `src/content/flow.ts` の該当回答を差し替えました。最終文：
  **「経験や年齢での線引きはしていません。毎日の決まったプログラムがないので、畑に行く日も、仕事をする日も、のんびり過ごす日もあってよく、自分で「今日は何をしよう」と考えながら暮らしてみたい方に向いています。反対に、予定や体験内容があらかじめ細かく決められたプログラムを求めている方には、少し合わないかもしれません。」**
- 同 item に任意の `link`（「詳しくは GUILD Farmとは」→ `/about/#fit`）を追加し、`src/components/sections/flow/FlowFaq.tsx` を **item ごとの link に対応**させました（`"link" in item` で持っているものだけリンクを出す最小限の追加。他の5問の表示は変わりません）。
- PLANS の FAQ（`src/content/plans.ts`）には向き不向きを問う質問がないため、変更していません（4問はいずれも期間・費用・設備・部屋の選び方）。

## 3. OWNER「どんな人に来てほしいか。」の地の文をトーン合わせ

- 松井さんの**引用2つは原文のまま維持**しています（`src/content/owner.ts` の `invitation.quotes`）。
- 地の文に「主体性」「求めています」といった採用要件的な表現はありませんでしたが、語調を揃えるため1文を追加しました：「毎日の決まったプログラムもありません。畑でできることは松井からお知らせしますが、畑に行く日も、仕事をする日も、のんびり過ごす日があってもいい滞在です。」

## 4. 「プログラムを案内し続ける」印象の文を修正

- `src/content/plans.ts`（1ヶ月滞在）
  - 「月・水・木・金は自由。畑に出たい日は、その都度**ご案内します**」→「月・水・木・金は自由。**畑でできることは、その都度松井からお知らせします**」
  - 注記「農作業のある日は、前日にご案内します。」→「**畑でできることは、前日に松井からお知らせします。参加するかどうかは自由です。**」
- 残した「ご案内」… `src/content/flow.ts` の2箇所（申し込み手続きの案内／松山までの経路の案内）は、滞在の過ごし方ではなく事務連絡なのでそのままです。
- `src/content/about.ts` の比較表にある「プログラム中心」は**一般的な農業体験の列**の文言（クライアント指定）なので変更していません。
- `src/content/copy.ts` `src/content/pages.ts` は編集禁止のため未変更です（`pages.ts` の「用意されたプログラムを消化するのではなく」は `outline` データ内で、画面には出力されていません。`out/` で0件を確認）。

## 確認したこと

- `npm run lint` … エラーなし。`npm run build` … 成功（9ページ静的生成）。
- `out/about/index.html` … 「こんな過ごし方を楽しめる方に。」／`id="fit"`／「少し合わないかもしれません」／ラベル「Your Own Pace」を出力。
- `out/flow/index.html` … 新しいFAQ回答と `href="/about/#fit"`（「詳しくは GUILD Farmとは」）を出力。
- `out/owner/index.html` `out/plans/index.html` … 追記・修正した文をそれぞれ出力。
- ブラウザでの目視確認は行っていません（指揮者側で確認）。

---

# Phase 3-C レビュー修正③

クライアントレビューの2点（HOME H-03 の見出しが不自然／「決まったプログラムがない」ことを魅力として伝える、STORIES 滞在記の追加予定を設計に記録）を反映しました（2026-09-14）。料金の数字は /plans/ のみ・資料にない事実は追加しない、というルールは維持しています。

## 1. HOME H-03 の見出し変更と、「決まったプログラムがない」を魅力として追加

- 見出し … `src/content/home.ts` の `values.title` を
  「この1週間、この1ヶ月に、起きること。」→ **「自分のペースで、農のある暮らしを。」** に変更しました。
  読んで意味がすぐ分かる見出しにする、という編集ルール2に沿って、対句・意味深コピーは使っていません。
- 導入文 … `values.sub`（1行）を **`values.paragraphs`（2段落）** に置き換えました。
  1. **「GUILD Farmに、毎日の決まったプログラムはありません。畑に出る日も、仕事をする日も、地域を巡る日も、のんびり過ごす日も、自分で決められます。畑でできることは、そのつど松井からお知らせします。」**
  2. 「畑に出て、その日採れたものを食べて、地域の人と顔を合わせる。滞在のあいだにできることは、だいたいこの5つです。」
  - 1段落目は注意書きではなく、この場所の良さとして書いています。内容は ABOUT の `fit`（「こんな過ごし方を楽しめる方に。」）と同じ趣旨・同じ事実で、矛盾させていません。事実の追加もしていません。
  - 2段落目は、下の5項目が何の一覧なのかの説明（旧 sub の役割）を引き継いでいます。「起きる」→「できる」に変えました。
- 表示 … `src/components/sections/home/HomeValues.tsx` を `sub`（単一文字列）から `paragraphs`（配列）の表示に変更しました。文字サイズ・色・余白は元のままで、段落間だけ `space-y-4` を追加しています。
- 5項目（土に触れる／食をつくる／自然の近くで働く／人とつながる／自分の暮らしを考える）・写真・リンク（「暮らし・過ごし方を見る」→ /life/）は**変更していません**。各項目の1行文に「〜ではなく」は含まれていませんでした。
- H-04 との重複整理 … H-04（`values` の次の `day`）の sub にあった「**決まった時間割はありません。**天気と…」を、
  **「朝に外へ出て、日中は自分の仕事をして、夕方もう一度。天気と、畑の都合と、その日の仕事の都合で、かたちは少しずつ変わります。」** に変更し、H-04 は「1日の流れ」だけを担当させました。どちらのブロックにも、役割を書いたコードコメントを入れています。
- `src/app/page.tsx` の並び一覧コメント（H-03 の行）も新しい見出しに合わせました。
- 「旅行は地域を見る。農業体験は農業を体験する。ここでするのは、そのどちらでもありません。」は、Phase 3-A/3-B の時点で HOME から無くなっており（`src/` `out/` ともに0件）、今回は削除対象がありませんでした。

## 2. STORIES / 滞在記を追加予定として設計に記録（実装はしない）

- **`docs/stories-plan.md` を新規作成**しました。記録した内容:
  - 目的（暮らしのイメージと信頼の補強）と、**架空の滞在者・体験談は作らないこと**。
  - 実装の前提となる取材・許諾の要点。出典は `guild-farm/sns/templates/interview-questions.md`（長尺3〜5分＋短答10〜30秒の2種類／「いいことだけ言わせない」）と `sns/content-planning.md`（掲載許諾が前提／**モニター滞在であることを隠さず「モニター参加者の声」と明示する**／料金の数字は出さない）。
  - 想定ルート `/stories/`・`/stories/<slug>/`、**ナビには当面入れない**こと、入口は HOME の H-08「この場所で生まれた言葉」（`HomeVoices.tsx`）の直下に「滞在記を読む →」を後から足す位置を確保すること（**今回リンクは置いていません**）。
  - データ構造案 `src/content/stories/<slug>.ts`（title / period / plan / profile（本人同意の範囲のみ）/ consent（文章・写真の許諾日・モニターか）/ body（Q&Aと段落の混在）/ photos（既存 `ImageAsset` 形式）/ published）。
  - 表示部品案（一覧カード＝`Photo`＋期間＋一言、記事＝`PageHeader`＋本文＋「滞在した時期のプラン」→ /plans/）。既存の `PageHeader` / `Photo` / `ClosingCta` を流用。
  - 公開前チェックリスト（実在の取材か・文章と写真の許諾・モニター明示・料金の数字なし・個人情報・本人確認・矛盾）。
- **ページ・ルート・コンポーネント・コンテンツファイルは作っていません。** `src/config/site.ts` の nav も変更していません。
- `README.md`「1. 作成したページ構成」の末尾に、今後追加予定としてこのドキュメントへの参照を1行足しました。

## 確認したこと

- `npm run lint` … エラーなし。`npm run build` … 成功。
- `out/index.html` … 「自分のペースで、農のある暮らしを。」と新しい導入文2段落を出力。「起きること。」「そのどちらでもありません」はともに**0件**。
- ブラウザでの目視確認は行っていません（指揮者側で確認）。

---

# 松井さんFB① 反映（2026-09-15）

一次情報: `feedback-matsui-1.md`（F1〜F6・横断チェック6項目・コピー編集上の注意）。
デザインの全面変更はしていません。事実関係と滞在イメージの修正が目的です。
固有名詞（体験先の農家さん・集落・相談会の名称）は書いていません。コミットはしていません。

## F1 1週目／2週目以降の実態

「毎日の決まったプログラムはありません」「基本的には自由」「自分で決められます」という単独の説明を、
**「最初の1週間はGUILD Farm側で農や地域を知る予定・体験を用意する → 2週目からは自分で予定を組み立てる →
やってみたいことがあれば、できる範囲で体験の機会を用意する」** に書き換えました。

- HOME `values.paragraphs`（H-03）／`stays.cards`（1週間・1ヶ月とも）／`closing`
- ABOUT `difference.practice`（見出しも変更、下記）／`fit`（#fit のid・note は据え置き）
- LIFE `header.lead` ／ `month.lead` ／ `month.blocks`（Week 1・Week 2–4 の見出しと本文）／
  `month.rhythm.note` ／ `week.lead`（1週間滞在の6日間は既存FACTSどおり。曜日別の中身は変えず、
  「日程はこちらで用意する」という前提だけ足しました）
- PLANS `options`（1ヶ月の本文・points／1週間の本文）
- FLOW `steps` 05「GUILD Farmへ」／FAQ「どんな人が向いていますか」
- OWNER `invitation.paragraphs`

「フリー」という語自体は一次情報どおりなので残し、「自分で予定を組み立てる日」という説明を添えました。

## F2 対象者（両方の人が想像できる文に）

「仕事を続けたまま」「都市にいるときと変わらない」「部屋の机に戻って仕事」型の単独訴求をやめ、
**「仕事を続けながら来る人も、まとまった時間をとって来る人も」** という書き方に統一しました。
年齢の決めつけ（20〜30代／働き盛り／定年後）は追加していません。「旅行では届かないところまで」は残しています。

- HOME HERO `body`「仕事を続けたまま、畑のある家に住む。」→
  **「仕事や暮らしを続けながらでも、まとまった時間を使ってでも。愛媛・松山の家に住んで、農のある暮らしを試してみる。」**
- HOME `values.items` 3番目 … 見出し「自然の近くで働く」→「仕事との距離を、自分で決める」
- ABOUT `header.lead`／LIFE `chapters` Work（見出し「仕事を、止めなくていい。」→「仕事との距離は、人それぞれ。」）
- PLANS `included`「仕事を続けられる環境」／FLOW FAQ（設問も「仕事を続けながら滞在できますか。」に）
- OWNER `invitation.paragraphs`
- `src/config/site.ts` の `description`（サイト共通のmeta。全ページとog:descriptionに出るため）

## F3 位置関係（1日2往復をやめ、日単位に）

- HOME `day` … 見出し「ここでの、1日。」→ **「畑に行く日の、1日。」**。
  朝＝家を出て公共交通機関で畑へ／昼＝畑での作業が続く／夕＝来た道を家へ戻る／夜＝自分の食事をつくる。時刻は書いていません。
- LIFE `day` … 同じ組み立てで全文版に。「畑から、1日がはじまる。」「もう一度、外に出る。」を廃止。
  `footnote` に「畑に行かない日は…」を足して、日単位であることを明示しました。
- 「畑のある家」「朝起きて畑へ行き、昼から仕事をして、夕方また外へ出る」は全ページから無くしました。
- 所要時間 … 梅津寺は **「電車で約45分」→「公共交通機関で約50分」** に統一し、**「梅津寺駅から徒歩約5分」** を追加。
  （LIFE `farm.places` / PLANS `fields` / FLOW `access.fields` / 旧 `copy.ts` の `places`）
  興居島（電車で約45分＋フェリーで10分）と道後の畑（自転車で10分）は既存のままです。
- 写真 … HOME・LIFE の「1日」の昼に使っていた机の写真（`dayWork`）を、畑の写真（`aboutField`）に差し替えました。
  畑にいる場面に机の写真が付くのを避けるためです。`dayWork` は HOME の5項目と LIFE の Work で引き続き使っています。

## F4 位置関係図（新規）

- **`src/components/ui/AccessMap.tsx` を新規作成**しました。
  今市シェアハウス（道後エリア）を起点に、道後の畑（自転車で10分）→ GUILD Farmの畑（公共交通機関で約50分・
  梅津寺駅から徒歩約5分／強調表示）→ 興居島の自然農園（電車で約45分＋フェリーで10分・毎週火曜）を縦に並べた模式図です。
- 線と着点だけをインラインSVG（`viewBox`付き・列の幅いっぱい）で描き、**地名と所要時間はHTML**（figure / figcaption）にしています。
  日本語フォントと折り返しをそのまま使うためで、375pxでも文字が潰れません。色は `globals.css` の
  CSS変数（`--color-sand` / `--color-clay` / `--color-paper`）をそのまま参照しています。
- **方位・縮尺・住所・番地・緯度経度は入れていません。実際の経路も描いていません。** figcaption に「模式図です」と明記しています。
  地図サービスの画面・地図画像の転載もしていません。
- 配置 … LIFE の農セクション（`LifeFarm.tsx`／体験の場所の一覧の直前）と PLANS の畑セクション（`PlansFields.tsx`／写真の下）。
- 文言は `life.ts` の `farm.map` と `plans.ts` の `fields.map` にあります（同じ内容を2か所に持っています。直すときは両方）。

## F5 収穫の確約表現

「その日採れたものを食べる」「自分が抜いた野菜を切る」「収穫したものを食べられる」を、
**「時期や畑の状況が合えば、採れたものを食べられる機会もあります」** 程度にそろえました。注意書きにはしていません。

- ABOUT `food` … 見出し「採れたものを、その日に食べる。」→ **「採れたものを、食べられる日。」**、sub・本文も同様。
  `header.lead`・`agrarian`・`difference.practice.examples`（暮らし）も同じ扱いに。
- HOME `values.items`「食をつくる」／`values.paragraphs` 2段落目／`day` の夜
- LIFE `day` の夜／`chapters` Food（見出し「自分が抜いた野菜を、その日に切る。」→「自炊をして、畑のものが並ぶ日もある。」）／
  `farm.paragraphs`（「来る時期と畑の具合によって、やることも採れるものもまるで違います」）
- PLANS `included`「畑に関わる時間」
- 写真のalt … `src/config/images.ts` の `dayDinner`「その日採れた野菜を並べた、屋外での食卓」→「収穫した野菜を並べた、屋外での食卓」。
  altは書き出したHTMLに出るため、ここだけ `images.ts` を触っています。

## F6 伊予柑（OWNER）

`owner.ts` の第4章「肥料を使わない、という決め方。」（`id="fertilizer"`）の、
味の理由の引用「嫌な雑味なく香りが強い味が好き美味しい」の直後に、次の1段落を追加しました。

> 大阪から愛媛へ移り住んだあとに食べた伊予柑が、自然栽培のものでした。嫌な雑味がなく、香りが強い。その味に、とても感動しました。

「人生で初めて食べた」「農園名」「誰にもらったか」「それが直接きっかけになった」は書いていません（コードコメントにも明記）。
既存の「味が好き」という理由と同じ場所に置いたので、矛盾しません。移住の時期・理由も書いていません。

## 横断チェックで追加で直した箇所

`feedback-matsui-1.md` の6項目で全6ページの content を grep・通読した結果、上記のほかに次を直しました。

- `src/config/site.ts` の `description` … 「畑のあるシェアハウス。仕事を続けたまま、1ヶ月暮らしてみる。」が
  全ページの meta description と og:description、404ページにも出ていたため書き換えました（F2・F3）。
- `src/content/pages.ts` の `life.description` … 「朝は畑から、昼は仕事、夕方また外へ。」が /life/ の meta description に
  出ていたため書き換えました。あわせて、画面には出ていない `outline`（旧セクション見出しの一覧）と `about.lead` のうち、
  F1/F3/F5 と食い違うものも新しい見出しに合わせています。
- `src/content/copy.ts`（旧コピー。データとしては残す方針のファイル） … **表示していないデータもクライアント側のJSに
  まとめて含まれる**ため、`out/_next/static/chunks/` に「畑のある家」「夕方また」「その日採れた」「続けたまま」
  「電車で約45分（梅津寺）」がそのまま残っていました。構造・キーは一切削らず、該当する文言だけを直しています
  （hero.body / about.paragraphs / about.pillars / aDay の4場面 / schedule.week1 / places の梅津寺 /
  experience の自炊 / faq の設問 / closing.body）。旧データの時刻（06:30 等）は残したままです。
- 見出しを変えたセクションについて、参照しているコードコメントも合わせました
  （`src/app/page.tsx` / `src/app/about/page.tsx` / `AboutDifference.tsx` / `AboutFit.tsx` / `AboutFood.tsx` /
  `HomeDay.tsx` / `LifeDay.tsx` / `LifeFarm.tsx` / `PlansFields.tsx`）。
- コピー編集上の注意 … 今回の書き換えで「ゆるやかに」「余白」「自分らしく」「そっと」「自然とつながる」「寄り添う」は
  増やしていません。触った周辺にあった「ゆるやかに（初日の体験）」「自分らしい、農との関わり方」は、
  それぞれ「畑で作業をしながら」「農との関わり方を、滞在のなかで見つける。」に直しました。

## あえて変更しなかったところ

- **ABOUT の比較表（`difference.compare`）** … 「自分のペースで関われる」「農・仕事・観光・暮らしを自由に組み合わせる」
  「自由度 ◎」はクライアント確定の文言で「増減・言い換えをしないでください」と指定があるため、そのままにしました。
  表の直後の `practice` 本文で、最初の1週間は予定を用意することを説明しています。**要判断**。
- **1週間滞在の曜日別（LIFE `week.days`）** … 月〜土の中身は既存FACTS v2どおりです。
  普遍化のルール（固有名詞を書かない）は維持し、事実は足していません。
- **旧 `copy.ts` の時刻（06:30 — 09:00 等）** … 表示していないデータなので残しています。
  `aDay` の本文だけを F3 に合わせました。
- **「1ヶ月滞在の起算日」「打上げBBQの日付」** … 一次情報で毎月固定かどうかが確認できていないため、
  今回も「滞在の最後」のままにしています。**要確認**。
- **梅津寺以外の所要時間の起点** … 「公共交通機関で約50分」は今市シェアハウスからの数字として書いています。
  興居島の「電車で約45分＋フェリーで10分」の起点は資料に明記がないため、模式図でも滞在先からの目安として並べ、
  figcaption に「所要時間は目安です」と添えました。**要確認**。
- **松井さん本人の写真・滞在者の写真** … 今回も追加していません（素材・許諾ともに未確認）。

## 確認したこと

- `npm run lint` … エラーなし。`npm run build` … 成功（9ページ）。
- `out/` の全文検索 … 「電車で約45分」は興居島の1件だけ（梅津寺は0件）。
  「畑のある家」「夕方また」「その日採れた」「続けたまま」「基本的に自由」「自分で決められ」「都市にいるとき」
  「もう一度、外」「自分が抜いた」は**すべて0件**。「公共交通機関で約50分」「徒歩約5分」「模式図」「伊予柑」は出力されています。
- ブラウザでの目視確認は行っていません（指揮者側・QAで確認）。

---

# 公開品質修正①（2026-09-15）— 内部文言の除去と ABOUT 比較表の実態あわせ

ページ構成・デザイン・他のコピーは変えていません。事実も足していません。

## 1. 制作・運営側の内部事情が見える文章を公開ページから除去

`out/*/index.html` を全文検索して、公開される文字列に残っていた
「※要確認」「確認中」「固まりきっていない」「準備中」を**すべて外しました**。
「決まっていない」という事実を隠すのではなく、「お問い合わせのときにご案内します」という
**ユーザー向けの案内**に置き換えています（情報量は減らしていません）。

### /plans/（`src/content/plans.ts`・4か所）

いずれも「条件が固まりきっていないため、サイトには書いていません／断定していません」を外しました。

- `header.lead[1]` … 初期費用とお支払い方法は、**お問い合わせのときにご案内します。**
- `included.note` … それ以外の費用の内訳は、**お問い合わせのときにご案内します。**
- `conditions.note` … これ以外の費用の内訳、初期費用、お支払い方法については、**お問い合わせのときにご案内します。**
- `faq.items[1].a` … それ以外に何がどこまで含まれるか、初期費用やお支払い方法については、**お問い合わせのときにご案内します。**

### /flow/（`src/content/flow.ts` `steps.notes`・3か所）

「※要確認」「受け入れ方法を確認中」「条件が固まりきっていない」を外しました。

- 申し込みの手続き（申込書の有無、お支払いの時期、キャンセルの扱い）は、**お問い合わせのときにご案内します。**
- 部屋や共用部の見学については、**ご相談ください。**
- 連絡の担当と、返信までの目安は、**お問い合わせのときにお伝えします。**

### / と /owner/（写真の注記・2か所）

「準備中」の部分だけを外し、写真の説明だけを残しました。
**差し替え予定の旨はコードコメントとして残しています**（`owner.ts` の
`★ 松井本人写真差し替え推奨` コメントはそのまま維持）。

- `src/content/home.ts` `owner.photoNote` … 「写真は、収穫した米。松井さん本人の写真は準備中です。」→ **「写真は、収穫した米。」**
- `src/content/owner.ts` `header.photoNote` … 「写真は秋の稲刈り。松井さん本人の写真は準備中です。」→ **「写真は秋の稲刈り。」**

### 内部管理用のリストを新規作成

- `docs/pending-facts.md` … 今回外した9項目（項目／元の文／現在の公開文／確認先）と、
  公開文には出ていないがコードコメントに残っている確認待ち10項目をまとめました。**非公開**です。
- 該当ファイルのコードコメントも、`docs/pending-facts.md` を参照する書き方に直しています
  （`src/content/flow.ts` / `src/app/flow/page.tsx`）。

## 2. ABOUT 比較表を運用実態（F1）と矛盾しない表現へ

`src/content/about.ts` の `difference.compare`。**GUILD Farm の列だけ**を変え、
「旅行」「一般的な農業体験」の列と軸名は据え置きです（自由度の行を除く）。

| 軸 | 旅行 | 一般的な農業体験 | GUILD Farm（修正前 → 修正後） |
|---|---|---|---|
| 滞在 | 数日程度 | 数時間〜1日 | 数週間〜数ヶ月（変更なし） |
| 農との関わり | 基本なし | 決められた体験に参加 | 自分のペースで関われる → **最初の1週間は予定を用意、その後は自分で組み立てる** |
| 過ごし方 | 観光中心 | プログラム中心 | 農・仕事・観光・暮らしを**自由に**組み合わせる → 農・仕事・観光・暮らしを**自分で**組み合わせる |
| 地域との関係 | 訪れる | 体験する | 暮らしながら知っていく（変更なし） |
| 自由度 | ○ → **自由** | △ → **決められた内容** | ◎ → **2週目からは自分で組み立てる** |

- 「自由度 ◎」は記号のままだと「完全に自由」と読めてしまうため、**この行だけ3列とも短い言葉**にしました。
  軸名「自由度」は、すぐ上に「過ごし方」の行があるため据え置いています。
- `AboutCompare.tsx` は記号前提の装飾をしていなかったため、**表示コードは変更なし**
  （PC は `md:table-fixed` で折り返し、スマホは軸ごとのブロックに積み替わります）。コメントだけ補足しました。
- 表の直下の `practice` 本文（「最初の1週間は、GUILD Farm側で農や地域を知るための予定を用意します。…
  2週目からは自分で予定を組み立てていきます。」）および `practice.note` と、表の中身が一致することを確認しました。

これにより、Phase 3-B の「あえて変更しなかったところ」にあった
**ABOUT 比較表の要判断は解消**しています。

## 変更したファイル

- `src/content/plans.ts` / `src/content/flow.ts` / `src/content/home.ts` / `src/content/owner.ts` / `src/content/about.ts`
- `src/content/copy.ts`（旧コピー） … 画面には出ませんが、Phase 3-B と同じく**クライアント側JSに含まれる**ため、
  `faq` に残っていた「条件が固まりきっていないため…」1件だけを直しました（構造・キーは変えていません）。
- `src/components/sections/about/AboutCompare.tsx`（コメントのみ） / `src/app/flow/page.tsx`（コメントのみ）
- `docs/pending-facts.md`（新規） / `CHANGELOG-phase3a.md`

## 確認したこと

- `npm run lint` … エラーなし。`npm run build` … 成功。
- `out/*/index.html` の全文検索 … 「要確認」「確認中」「固まりきって」「未確定」「未確認」「準備中」「検討中」「整い次第」は
  **すべて0件**（コードコメントはビルド出力に含まれないため対象外）。
- 料金の数字（85,300 / 58,300 / 25,000 / 35,000）は **/plans/ のみ**に出ています。
- Phase 3-B で0件にした禁止語（「畑のある家」「夕方また」「その日採れた」「続けたまま」「基本的に自由」
  「自分で決められ」「都市にいるとき」「もう一度、外」「自分が抜いた」）は**0件のまま**増えていません。
- コミットはしていません。ブラウザでの目視確認も行っていません。

---

# ABOUTブランド編集（2026-09-15・クライアント確定）

指示書: `about-brand-edit.md`。ABOUTを「GUILD Farmが何をする場所かを理解するページ」に整理し、
人物ページの呼び方をサイト全体で「オーナー松井」に統一しました。
デザインシステム・ブランドカラー・共通部品は変更していません。写真も既存素材のみです。
**見出しと本文は、クライアントが書いた文をそのまま置いています**（独自のブランドコピーは作っていません）。

## 1. ABOUT の再構成（6セクション）

| # | id | 見出し | 中身 |
|---|---|---|---|
| — | — | GUILD Farmとは | 指定の導入4行（PageHeader。sub は置かず、4行をリードにしました） |
| 01 | `agrarian` | 「農的暮らし」って、どんな暮らし？ | 指定本文＋既存の比較表（深緑の面。旧 AboutDifference を置き換え） |
| 02 | `fit` | 最初の1週間は、一緒に。／その先は、自分で組み立てる。 | 滞在の過ごし方。**id は変更していません**（/flow/ FAQ の着地点） |
| 03 | `comfort` | 農的暮らしを通じて、“心地イイ”を見つめ直す。 | HOME HERO と同じコピー・同じ2行組み |
| 04 | `food` | 農と食が、少し近くなる。 | 収穫は確約しない書き方のまま（F5） |
| 05 | `people` | 地域の人と出会う。 | 資料にある範囲だけ |
| 06 | `future` | この先につくりたいもの。 | **id="future" 維持**。末尾に「オーナー松井」への導線 |
| — | — | 少しだけ、違う暮らしをしてみる。 | 既存 ClosingCta（次に読む: /life/） |

- 見出しの2行組みは `src/lib/jp.tsx` に `phraseLines()` を追加して共通化しました
  （HERO の `heroTitle()` を置き換え。**HOMEの見え方は変わりません**）。
  03「“心地イイ”」の見出しは行を折り返さない組み方なので、HEROと同じく
  `clamp(1.6rem, 3.2vw, 2.75rem)` まで下げています（スマホ375pxで横スクロールを出さないため）。
- 指定の位置で改行する見出しのために、`ui/SectionHeading` に任意の `lines` prop を足しました。
- 比較表は残し、「滞在」の行だけ **数週間〜数ヶ月 → 1週間、または1ヶ月** に直しました
  （実際のプランと合っていなかったため）。他の行は据え置きです。
- 写真は各セクション1〜2枚に絞りました（aboutSoil / expNature / journal[1] / expFood /
  expHarvest / expPeople / dayEvening / whyVisual）。
  掲載許諾が未確認の journal[3]（地域の子ども・`docs/pending-facts.md` B-5）は、この整理で**使わなくなりました**。

## 2. 「オーナー松井」へ移した一次情報（ABOUTから削除していません）

| ABOUTにあった内容 | 移設先（`src/content/owner.ts`） |
|---|---|
| 無肥料の理由（環境）「今の時代に沿ったカタチで地球に愛される人間の在り方を追求したかった」 | 04 `fertilizer` の章（原文引用として追加） |
| 「豆科＋ひまわり＋水＋ミネラル＋炭が今の答え」 | 04 `fertilizer` の章（原文引用として追加） |
| 生産性の低さ・木々を枯らさずに育てること＝いまも試行錯誤の途中 | 05 `spiral` の章（一文を追加） |
| 高齢化・後継者・相場・薄利多売・付加価値（旧 ABOUT 03「なぜ、農のある暮らしなのか」） | 08 `rethink` の章（1段落として追加） |
| 「食生産を担う人が減る一方でじゃーどうすれば？…」 | 08 `rethink` の章（原文引用として追加） |
| 味／伊予柑／生産性と価格の悩み／2011年東北／移住 | **すでに owner にあったため、追加していません**（二重掲載の回避） |
| 不安や恐怖→豊かさ・心地よさ／「1ヶ月という貴重なじかん…」 | **すでに owner 09 `now` にあったため、ABOUT側を削除** |

`out/` の全文検索で、旧ABOUTの一次情報34項目がすべてサイト内のどこかに残っていることを確認しました
（消えた一次情報はありません）。

## 3. 削除した重複・AI的な表現

- 「見るのでも、体験するのでもなく。」「旅行ではなく、暮らしてみる。」…「AではなくB」型の見出し
- 「増やしたいのは、農に関わる人です。」…余韻だけの短文
- 「だから、危機感から来てもらう場所にはしていません。」…対比だけの段落
- 「たとえば（農／仕事／暮らし／学び／観光）」の5項目 … /life/ と同じ内容だったため（LIFEに残っています）
- 「少し足を伸ばせば、竹林も、田んぼも、海も」「毎晩みんなで食卓を囲む決まりはありません」 … /life/ ・/flow/ と重複
- 旧セクション部品を削除: `AboutDifference.tsx` / `AboutWhy.tsx` / `AboutFertilizer.tsx` / `AboutAbundance.tsx`

## 4. 「オーナー松井」表記の統一（URL /owner/ は変更なし）

| 場所 | 変更前 → 変更後 |
|---|---|
| `src/config/site.ts` navItems（ヘッダー・モバイルメニュー・フッター共通） | 松井について → **オーナー松井** |
| `src/content/pages.ts` owner.metaTitle（OGPも同じ値） | 松井について｜この場所を、はじめた人 → **オーナー松井｜この場所を、はじめた人** |
| `src/content/pages.ts` owner.description | 冒頭に「GUILD Farmのオーナー松井のこと。」を追加 |
| `src/content/pages.ts` owner.title | この場所を、はじめた人。 → **オーナー松井** |
| `src/content/owner.ts` header.title（h1） | この場所を、はじめた人。 → **オーナー松井**（肩書＋氏名の sub はそのまま） |
| `src/content/home.ts` owner.link（H-05） | この場所をつくった人 → **オーナー松井**（見出しは確定コピーのため変更なし） |
| `src/content/home.ts` voices.link（H-08） | 松井について → **オーナー松井** |
| `src/content/about.ts` future.link | （旧 why.link「松井について」）→ **オーナー松井** |

- HOME H-05 の見出し「この場所を、はじめた人。」は確定コピーなので変更していません（リンクのラベルのみ）。
- 本文中で人として言及する箇所（「松井さん」「松井が」）はそのままです。
- `src/content/pages.ts` about も新しい役割に合わせて更新しました
  （metaTitle「GUILD Farmとは｜農的暮らしを、実際の暮らしの中で試してみる場所」／description は導入文の要約／outline 6件）。

## 変更したファイル

- `src/content/about.ts`（全面書き換え） / `src/content/owner.ts` / `src/content/pages.ts` / `src/content/home.ts` / `src/config/site.ts`
- `src/app/about/page.tsx` / `src/app/owner/page.tsx`（コメント）
- `src/components/sections/about/` … `AboutAgrarian.tsx`（書き換え）／`AboutFit.tsx`／`AboutComfort.tsx`（新規）／
  `AboutFood.tsx`／`AboutPeople.tsx`／`AboutFuture.tsx`／`AboutCompare.tsx`（参照先のみ）、
  `AboutDifference.tsx`・`AboutWhy.tsx`・`AboutFertilizer.tsx`・`AboutAbundance.tsx` は削除
- `src/components/sections/Hero.tsx`（共通化のみ・表示は変化なし） / `src/components/sections/home/HomeOwner.tsx`（コメント）
- `src/components/ui/SectionHeading.tsx`（任意の `lines` prop を追加） / `src/lib/jp.tsx`（`phraseLines()` を追加）
- `CHANGELOG-phase3a.md`
- `docs/pending-facts.md` は変更していません（未確定情報を公開文に戻していないため）。

## 確認したこと

- `npm run lint` … エラーなし。`npm run build` … 成功（9ページ）。
- `out/about/index.html` … 指定の見出し7つがすべて出力されている。料金の数字0件、
  「振興協議会」0件、「準備中」「要確認」「確認中」0件。
- `out/` 全体 … 「松井について」「この場所をつくった人」ともに0件。
- 内部リンク … 全ページの `href` を抽出し、`/about/` `/owner/` `/life/` `/plans/` `/flow/` `/` と
  `#future` `#fit` `#tohoku` などのアンカー先がすべて存在することを確認（リンク切れなし）。
- コミットはしていません。ブラウザでの目視確認も行っていません（PC1280／スマホ375の確認はQAで）。

---

# 松井についてブランド編集（2026-09-16・クライアント確定）

指示書: `owner-brand-edit.md`。棚卸し: `docs/owner-inventory.md`。
/owner/ を「プロフィール紹介」から「松井さんの実体験と本人の言葉でGUILD Farmに至るまでを伝えるページ」へ
再構成し、あわせて前回の「オーナー松井」ページ名称化を **「松井について」** に戻しました。
デザインシステム・ブランドカラー・共通部品は変更していません。写真も既存素材のみです。
URL `/owner/` は変更していません。HOME・ABOUT・LIFE・PLANS・FLOW の確定コピー／構成／料金は変更していません。

## 0. ページ名称の戻し（横断修正）

「オーナー松井」は**文章中の人物表現**であり、ページ名称・プロフィール名ではありません。

| 場所 | 変更前 → 変更後 |
|---|---|
| `src/config/site.ts` navItems（PC/モバイルナビ・フッター共通） | オーナー松井 → **松井について** |
| `src/content/pages.ts` owner.metaTitle（`<title>`／OGP） | オーナー松井｜この場所を、はじめた人 → **松井について｜GUILD Farm オーナー 松井 真弥** |
| `src/content/pages.ts` owner.description（meta／OGP） | 「GUILD Farmのオーナー松井のこと。…」 → 新しい章立ての要約に書き換え |
| `src/content/pages.ts` owner.title / `src/content/owner.ts` header.title（h1） | オーナー松井 → **松井について** |
| `src/content/pages.ts` owner.sub / `src/content/owner.ts` header.sub（プロフィール表記） | 松井 真弥（GUILD Farm オーナー） → **GUILD Farm オーナー　松井 真弥**（肩書＋氏名） |
| `src/content/home.ts` owner.link（H-05）／voices.link（H-08） | オーナー松井 → **松井について** |
| `src/content/about.ts` future.link | オーナー松井 → **松井について** |

- HOME H-05 の見出し「この場所を、はじめた人。」は確定コピーなので変更していません（リンクのラベルのみ）。
- ABOUT 本文の「オーナー松井が取り組んできた農」「オーナー松井の話や〜」「オーナー松井が10年ほど温めてきた」の
  3か所は、**文章中の人物表現なのでそのまま残しています**。

## 1. 章立ての再構成（指示書 2〜11）

| # | id | 見出し | 中身 |
|---|---|---|---|
| — | — | 松井について | PageHeader。プロフィール＋簡潔な導入2行（人生を要約しません） |
| — | `timeline` | これまでのこと。 | 年表ナビ6件。**読む順（章の順）**に並べ替え。「10年前」は相対表記のまま |
| 01 | `ehime` | 愛媛で、農と出会う。 | 大阪→愛媛、自然栽培の伊予柑（F6）＋味の原文引用 |
| 02 | `fertilizer` | 肥料を使わない農業へ。 | 環境・備え・「豆科＋ひまわり…」。ABOUT移設分を統合 |
| 03 | `spiral` | うまくいかなかった、5年間。 | 生産性／木々／手間／価格／生産以外の仕事。成功物語にしない |
| 04 | `rethink` | 農業そのものについて、考える。 | 高齢化・後継・相場・薄利多売・付加価値・「じゃーどうすれば？」（ABOUT移設分を原文引用に戻す） |
| 05 | `village` | 東北と、19世帯の集落での暮らし。 | 2011年4月／19世帯の集落／美味しい米／心配。原文を主役に |
| 06 | `years` | 2015年、そして2020年。 | フリーペーパー／ロックダウン／「何にもできてへんと凹みもしました」 |
| 07 | `now` | 不安や危機感から、“心地イイ”へ。 | 章サブに確定コピー、本文は原文引用1つだけ |
| 08 | `guild` | GUILD Farmを始める。 | 「カタチとして実現できてきました」＋何をする場所か |
| 09 | `invitation` | どんな人に来てほしいか。 | 原文引用2つ＋F1/F2 の範囲の地の文（既存） |
| 10 | `voices` | 滞在した人の言葉 | 確認済みの3つのみ・属性なし（既存） |
| 11 | `future` | これから。 | **新規セクション**。「畳み1畳の畑」「半径2kmの農的社会コミュニティ」を本人の原文で |
| — | — | 少しだけ、違う暮らしをしてみる。 | 既存 ClosingCta。**次に読む先を /plans/ → /life/ に変更** |

- 各章は「事実（短い地の文）→ 本人の原文引用 → 次の事実」の形にそろえました。
- 旧章 `tohoku` / `harvest` は `village` に統合（同じエピソードのため）。旧 `y2015` / `y2020` は `years` に統合。
- 新規部品: `src/components/sections/owner/OwnerFuture.tsx`。
- `src/content/owner.ts` に `future` を追加、`OwnerPhotoKey` を写真の変更に合わせて入れ替えました。

## 2. 削除したAIによる意味づけ・重複（一次情報は削除していません）

- 「この問いが、何年かたってから別のかたちで戻ってきます。」…本人の言葉にない伏線的な意味づけ
- 「2011年に東北で感じたことが、ここで結びついています。」…「すべてが一本につながりました」型
- 「いまGUILD Farmで伝えようとしているのは、この時期に身についた感覚です。」…「この経験こそが原点です」型
- 「焦ったときに気づいたのは、…」…「そこで気づいたのです」型
- 「心配していたことと、やっていたことが、ずれていました。」…指示書1の使用禁止表現
- 「心配していたのは食生産を担う人が減っていくことだったのに、やっていたのは自分の農業を成り立たせることでした。」…「Aではなく、B」型
- 「はじまりは、ここでした。」「理由は三つあります。味、環境、そして備え。」「頭のなかにあったことを、外に出そうとしていた時期でした。」…制作側による総括
- 「やってみると、米は想像よりずっと手がかかります。…暮らしの重心が少し変わりました。」…原文にない体験描写
- ABOUT から移設した農業の課題の段落は、**制作側の言い換えをやめて原文引用に戻しました**（内容は同じ・原文のほうが情報量が多い）。

## 3. 写真

- 人物の顔が大きく写り「この人が松井さん」と誤解されうる写真を、このページから外しました。
  - `dayMorning`（→ `OwnerInvitation` は `expFood` に差し替え）
  - `dayDinner` / `journal[5]`（旧 `now` / 旧 `village`。いずれも手前に人物の顔が大きく写るため不使用）
- 使用中: `hero`（年表）／`expHarvest`（愛媛）／`aboutSoil`（無肥料）／`aboutField`（5年間）／
  `whyVisual`（農業観）／`expSeason`（集落）／`expNature`（今）／`expFarmwork`（GUILD Farm）／`expFood`（どんな人に）。
- `expPeople` / `journal[3]` / `owner.jpg` は引き続き不使用です。
- ★ 松井本人写真差し替え推奨のコードコメントは維持しています（公開文には書いていません）。

## 確認したこと

- `npm run lint` … エラーなし。`npm run build` … 成功（9ページ）。
- `out/owner/index.html` … 指示書1の禁止句（「気づいたのです」「一本につながり」「原点です」「かもしれません」
  「ずれていました」「ではなく、」）**すべて0件**。「振興協議会」「準備中」「要確認」「確認中」**0件**。
  料金の数字（85,300 / 58,300 / 25,000 / 35,000）**0件**。「心地イイ」は章07の見出しとサブの**2か所のみ**。
- 引用照合 … `src/content/owner.ts` の引用文字列25本すべてが `matsui-story.txt` と一致
  （全角「､」「｡」「｢」「｣」の置換と改行の除去のみ）。章07のサブだけはクライアント確定コピーです。
- 一次情報の消失チェック … `docs/owner-inventory.md` の KEEP / MOVE / MERGE 項目37件が
  すべて `out/owner/index.html` に出力されていることを確認。
- 全6ページ … 「オーナー松井」はABOUT本文の人物表現3か所のみ。
  ページ名称・ナビ・リンクラベル・`<title>`・meta description・OGP としての使用は**0件**。
- 年表ナビのアンカー（#ehime / #fertilizer / #spiral / #village / #years / #now）はすべて存在（リンク切れなし）。
- `docs/pending-facts.md` は変更していません。コミット・ブラウザ操作はしていません。

## 変更したファイル

- `src/content/owner.ts`（全面書き換え）／`src/content/pages.ts`（owner）／`src/content/home.ts`（リンクラベル）／
  `src/content/about.ts`（リンクラベル）／`src/config/site.ts`（navItems）
- `src/app/owner/page.tsx`（構成＋コメント）／`src/app/about/page.tsx`（コメントのみ）
- `src/components/sections/owner/OwnerFuture.tsx`（**新規**）／`OwnerChapters.tsx`（写真の対応表）／
  `OwnerInvitation.tsx`（写真差し替え）／`OwnerTimeline.tsx`（コメント）／`OwnerVoices.tsx`（コメント）
- `src/components/sections/about/AboutFuture.tsx`（コメントのみ）／`src/components/sections/home/HomeOwner.tsx`（コメントのみ）
- `docs/owner-inventory.md`（**新規**）／`CHANGELOG-phase3a.md`
